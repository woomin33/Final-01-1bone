// LiveProgress.stories.tsx

import { LiveProgress } from './LiveProgress';
import React from 'react';

const meta = {
  title: 'Components/LiveProgress',
  component: LiveProgress,
};

export default meta;

export const Default = {
  render: () => <LiveProgress />,
};
